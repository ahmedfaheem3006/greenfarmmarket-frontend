RewriteEngine On

# Redirect all requests for non-existent files or directories to index.html for React Router SPA
RewriteCond %{REQUEST_FILENAME} !-f
RewriteCond %{REQUEST_FILENAME} !-d
RewriteRule ^ index.html [L]
